# requirements.txt
# 企业数据分析智能体 - 依赖清单

# ---------- Web 框架 ----------
Flask==3.1.0
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1

# ---------- 数据库驱动 ----------
# 默认 SQLite（内置），额外支持 MySQL/PostgreSQL 可选
pymysql==1.1.1
psycopg2-binary==2.9.10

# ---------- 认证与安全 ----------
Werkzeug==3.1.3          # 密码哈希（Flask 依赖）
Pillow==11.1.0           # 验证码图片生成

# ---------- 大模型交互 ----------
requests==2.32.3         # HTTP 请求（API/Ollama）

# ---------- 向量计算与检索 ----------
numpy==2.2.3
faiss-cpu==1.10.0        # 向量相似度检索（可选，也可用 scipy 替代）
scipy==1.15.2            # 余弦相似度计算（备选）

# ---------- 数据导出 ----------
pandas==2.2.3
openpyxl==3.1.5          # Excel 写入
python-docx==1.1.2       # Word 生成
python-pptx==1.0.2       # PPT 生成
Jinja2==3.1.6            # HTML 模板渲染（Flask 依赖）

# ---------- 沙箱代码执行 ----------
# 白名单模块已在代码中硬编码，无需额外安装（pandas, numpy 等已包含）

# ---------- 可选：更强大的向量存储 ----------
# chromadb==0.4.22       # 如未来升级长期记忆存储，可切换至 Chroma

# ---------- 开发与调试 ----------
# ipython==8.18.1        # 交互式调试（可选）

cryptography==42.0.5